name: CI

on:
  push:
    branches: ['main']
  pull_request:
    branches: ['main']
    types: [opened, synchronize, reopened]

jobs:
  project-build-test:
    uses: asimov-platform/.github/.github/workflows/npm-ci.yml@master
    with:
      skip-tests: true
